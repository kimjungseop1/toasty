<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature
        android:name="android.hardware.camera.ar"
        android:required="true"/>

    <application
        android:name=".ToastyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MotionPic"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.ar.core"
            android:value="required" />

        <activity
            android:name=".ui.component.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.component.home.MainActivity"
            android:exported="true" />

        <activity
            android:name=".ui.component.login.main.LoginActivity"
            android:exported="true" />

        <activity
            android:name=".ui.component.join.main.JoinEmailActivity"
            android:exported="true" />

        <activity
            android:name=".ui.component.join.consent.JoinConsentActivity"
            android:exported="true" />

        <activity
            android:name=".ui.component.join.welcome.WelcomeActivity"
            android:exported="true" />

        <activity
            android:name=".ui.component.join.term_privacy.PolishWebActivity"
            android:exported="true" />

        <activity
            android:name=".ui.component.login.find_pw.FindPwActivity"
            android:exported="true" />

        <activity
            android:name=".ui.component.join.email_cert.CertEmailActivity"
            android:exported="true" />

        <activity
            android:name=".ui.component.login.main.LoginPwActivity"
            android:exported="true" />

        <activity
            android:name=".ui.component.home.ar.ArCameraActivity"
            android:exported="true" />

    </application>

</manifest>